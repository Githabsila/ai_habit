# ADAM V25 — Level Ring Geometry Fix

Исправлено смещение центрального блока уровня.

- SVG-кольцо и блок `29 / LVL` теперь имеют одну геометрическую точку центра.
- Кольцо центрируется через `left:50% / top:50%` + `translate(-50%,-50%)`.
- Текст уровня центрируется тем же способом.
- Убрано влияние предыдущих `inset/transform` правил и flex-сжатия.
- Остальной дизайн не изменён.
