# Зависимости для разработки/тестов — НЕ ставятся в продакшен (Railway
# использует только requirements.txt). Установка: pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0

# Проверка зависимостей на известные CVE (безопасность): pip-audit -r requirements.txt
pip-audit==2.10.1
